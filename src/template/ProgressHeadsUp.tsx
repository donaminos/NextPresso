import { Alert } from "@/template";

export const ProgressHeadsUp = () => {
  return (
    <Alert title="Heads up!">
      This project is still in the early stages and is a work in progress. Thank
      you for your patience.
    </Alert>
  );
};
